pyyaml
requests
pillow
# Optional — these are not exercised by the scaffold but will be needed when real
# signals and capture land. They are listed here so a single `pip install` sets up
# a working environment for later passes.
playwright
opencv-contrib-python
trueskill
# VLM judge signal (pipeline/signals/vlmjudge.py). anthropic is the default judge
# (models.judge: opus). openai is needed for gpt-* judge tiers AND for the Nemotron
# signals (stress_test, prompt_consistency) — Nebius Token Factory is OpenAI-compatible.
anthropic
openai
# Saliency signal (pipeline/signals/saliency.py): DeepGaze IIE/III on PyTorch.
torch
torchvision
deepgaze_pytorch @ git+https://github.com/matthias-k/DeepGaze.git
