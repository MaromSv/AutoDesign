# Copy to .env and fill in. docker-compose loads this automatically.

# Required: vlm_judge calls the Anthropic API.
ANTHROPIC_API_KEY=sk-ant-...

# Optional: only needed if you turn on the Nemotron signals in config.yaml.
# NEBIUS_API_KEY=

# Optional logging level (DEBUG, INFO, WARNING).
# LOG_LEVEL=INFO
